# Weights & Biases Configuration
# Set the wandb project name for training and evaluation runs
# If not set, defaults are used:
#   - Training: "gnn-network-routing"
#   - Evaluation: "gnn-network-routing-eval"

WANDB_PROJECT=gnn-network-routing

# Optionally, you can also set wandb entity (team/username)
# WANDB_ENTITY=your-entity

# Optionally, you can set wandb API key here (not recommended, use wandb login instead)
# WANDB_API_KEY=your-api-key
